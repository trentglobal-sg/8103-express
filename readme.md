# How to initialize a NodeJS application

1. You are in an empty directory
2. Execute `npm init -y` (per new project)
3. Execute `npm install express` (per new project)
4. Create your own `index.js`
5. Execute `node index.js` to start server